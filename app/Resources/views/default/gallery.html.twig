{% extends 'base.html.twig' %}

{% block body %}
<div class='container'>
	{% for path in paths %}
		<div class="col-xs-4 col-sm-3 col-md-3 col-lg-3">
        		<a href="#" class="thumbnail">
          			<img src={{ asset(path) }}>
        		</a>
        	</div>
	{% endfor %}
</div>
{% endblock %}